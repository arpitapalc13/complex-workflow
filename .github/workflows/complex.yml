Skip to content
Navigation Menu
arpitapalc13
complex-workflow

Type / to search
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
.github/workflows/complex.yml
Create complex.yml #1
Jobs
Run details
Workflow file for this run
.github/workflows/complex.yml at 1ea61e7
name: Complex
 Check failure on line 1 in .github/workflows/complex.yml


GitHub Actions
/ .github/workflows/complex.yml
Invalid workflow file

Invalid `steps` value - steps should be list of `uses` or `run` items

on: push

jobs:
  ubuntu:
    runs-on: ubuntu-latest
    steps:
      -run: date
      
  windows:
    runs-on: windows-latest
    steps:
      -run: date
      
  macos:
    runs-on: macos-latest
    steps:
      -run: date
      
  depends:
    needs: [ubuntu, windows, macos]
    runs-on: windows-latest
    steps:
      -run: date
Create complex.yml · arpitapalc13/complex-workflow@1ea61e7  
